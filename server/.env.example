# =================================================================
# Environment Variables Configuration
# =================================================================
# 
# Instructions:
# 1. Copy this file to .env (cp .env.example .env)
# 2. Replace the placeholder values with your actual values
# 3. Never commit the .env file to version control
#
# =================================================================

# DATABASE CONFIGURATION
# MongoDB Atlas connection string
# Get this from: MongoDB Atlas → Clusters → Connect → Connect your application
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<dbname>?retryWrites=true&w=majority
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority

# SERVER CONFIGURATION
# Port for the backend server (default: 3001)
PORT=3001

# CORS CONFIGURATION
# Comma-separated list of allowed origins
# For development only: http://localhost:8080
# For production only: https://your-frontend-domain.vercel.app
# For both: http://localhost:8080,https://your-frontend-domain.vercel.app
# For all origins (not recommended): *
CORS_ORIGIN=http://localhost:8080

# NODE VERSION (Optional - for deployment)
# Specify Node.js version for deployment platforms
NODE_VERSION=18
